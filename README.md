# hoi.hrysd.org

```
$ rails new . --skip-bundle -d postgresql
```

```
$ brew install libpq
$ export PATH="/path/to/homebrew/opt/libpq/bin:$PATH"
$ bundle install
$ docker compose up -d
$ bin/rails s
```
